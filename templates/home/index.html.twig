{% extends 'base.html.twig' %}

{% block title 'Liste des freelancers'%}

{% block body %}

    {% include 'home/_search_section.html.twig' %}

    <section class="shop-section section-padding" id="section_3">
        <div class="container">
            <div class="row">

                <div class="col-lg-12 col-12">
                    <small class="section-small-title mt-2 mb-4">Liste des Freelancers (<span class="tooplate-red">{{ missions.getTotalItemCount }}</span>)</small>

{#                    <h2 class="mt-2 mb-4"><span class="tooplate-red">Interior</span> Products</h2>#}
                </div>

                {% for mission in missions %}
                    <div class="col-lg-4 col-12">
                        <div class="shop-thumb">
                            <div class="shop-image-wrap">
                                <a href="{{ path('mission_show', { slug: mission.slug }) }}">
                                    <img class="shop-image img-fluid" src="{{ mission.imagePath|imagine_filter('thumbnail_homepage') }}" alt="Image {{ mission.title }}">
{#                                    <img src="{{ asset('./assets/images/shop/green-sofa-white-living-room-with-blank-table-mockup.jpg') }}" class="shop-image img-fluid" alt="">#}
                                </a>

                                <div class="shop-icons-wrap">
                                    <div class="shop-icons d-flex flex-column align-items-center">
                                        {% if mission.user.avatar %}
                                            <img src="{{ mission.user.imagePath|imagine_filter('thumbnail_small') }}" class="rounded-circle" alt="Logo {{ mission.user.nickname }}">
                                        {% else %}
                                            <i class="fa fa-user fa-3x rounded-circle" ></i>
                                        {% endif %}

{#                                        <a href="#" class="shop-icon bi-heart"></a>#}

{#                                        <a href="#" class="shop-icon bi-bookmark"></a>#}
                                    </div>

                                    <p class="shop-pricing mb-0 mt-3">
                                        <span class="badge custom-badge">{{ mission.price }} €</span>
                                    </p>
                                </div>

                                <div class="shop-btn-wrap">
                                    <a href="{{ path('mission_show', { slug: mission.slug }) }}" class="shop-btn custom-btn btn d-flex align-items-center align-items-center">Plus d'infos</a>
                                </div>
                            </div>

                            <div class="shop-body">
                                <h4>{{ mission.title }}</h4>
                            </div>
                        </div>
                    </div>
                {% endfor %}

                <div class="col-lg-12 col-12">
                    <nav aria-label="Page navigation example">
                        <ul class="pagination justify-content-center">
                            {{ knp_pagination_render(missions) }}
                        </ul>
                    </nav>
                </div>

            </div>
        </div>
    </section>
{% endblock %}

{% extends 'admin.html.twig' %}

{% block title %}Modifier {{ tag.name }}{% endblock %}

{% block body %}
    <h1>{{ block('title') }}</h1>

    {{ include('admin/tag/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_tag_index') }}">Retour</a>

    {{ include('admin/tag/_delete_form.html.twig') }}
{% endblock %}
